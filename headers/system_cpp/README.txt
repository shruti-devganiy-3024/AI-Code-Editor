This folder is for standard/system C and C++ header references.
The editor reads system headers from the compiler environment (g++, clang++) rather than storing them locally.
This is intentional to keep the project lightweight and fully offline-compatible.
